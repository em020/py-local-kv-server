# Abstract base models like the DeclarativeBase and technical Mixins (ID, Timestamps) and other shared data structures can be defined here.
# to avoid circular imports and to provide a single source of truth for these foundational components.

# Example: A TimestampMixin that adds created_at to every table.
